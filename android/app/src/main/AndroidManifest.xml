<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13+ (API 33) will not deliver a single notification until the
         user has granted this at runtime, and it fails SILENTLY: no exception,
         no callback, nothing in logcat. The OneSignal SDK's own manifest merges
         this permission in, so it was almost certainly present in the merged
         output already, but relying on a dependency's manifest for the one
         permission the entire feature depends on is not a thing to leave
         implicit. Declared here so it is visible in review and in code search. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Networking for the snapshot, OneSignal push, and store/version checks.
         Plugins merge this in already, but the app's core function depends on
         it, so it is declared explicitly rather than left implicit. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Lets flutter_local_notifications re-register the daily learn reminder
         after a device reboot, so a scheduled nudge is not silently dropped
         until the app is next opened. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="Fructa"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:enableOnBackInvokedCallback="true">

        <!-- Fructa gold. OneSignal reads this to tint the small icon and the
             notification accent. ARGB, no leading hash. Without it the status
             bar icon renders in the platform default grey. -->
        <meta-data
            android:name="com.onesignal.NotificationAccentColor.DEFAULT"
            android:value="FFE7B24C" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Package visibility. From Android 11 (API 30) an app cannot see another
         app's activities unless it declares an intent it intends to resolve. If
         it cannot see them, canLaunchUrl returns false and url_launcher does
         nothing at all: no crash, no toast, just a dead button. The logcat tell
         is "component name for https://... is null".
         https://developer.android.com/training/package-visibility -->
    <queries>
        <!-- Browsers, for the broker Trade button, the official-site links and
             every prospectus/factsheet URL in the app. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>

        <!-- Dialer and mail client, for the Company Contact section. Declared
             now so the section does not ship broken on Android 11+ the day it
             lands. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>

        <!-- Required to query activities that can process text, see:
             https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.
             In particular, this is used by the Flutter engine in
             io.flutter.plugin.text.ProcessTextPlugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>